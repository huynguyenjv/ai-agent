# === Cloud VM Server ===

# Core framework
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.6

# LangGraph agent orchestration
langgraph>=0.2
langchain-core>=0.3

# Vector database
qdrant-client>=1.12

# Dense embedding model
sentence-transformers>=3.3

# BM25 sparse vector computation
rank-bm25>=0.2

# vLLM client (OpenAI-compatible)
openai>=1.54

# Environment
python-dotenv>=1.0

# YAML parsing (for rules engine)
pyyaml>=6.0

# Prometheus metrics
prometheus-client>=0.20

# HTTP client
httpx>=0.27

# PostgreSQL (async)
asyncpg>=0.29
psycopg[binary,pool]>=3.1

# === Testing ===
pytest>=8.0
pytest-asyncio>=0.23

# === MCP Server (Client Machine) ===

# MCP protocol
mcp>=1.0

# Tree-sitter parsing
tree-sitter>=0.23
tree-sitter-java
tree-sitter-go
tree-sitter-python
tree-sitter-c-sharp
tree-sitter-javascript
tree-sitter-hcl

redis>=5

# Optional: OpenTelemetry tracing (Phase 14.1). Uncomment + set OTEL_EXPORTER_OTLP_ENDPOINT.
# opentelemetry-sdk>=1.25
# opentelemetry-exporter-otlp-proto-grpc>=1.25
# opentelemetry-instrumentation-fastapi>=0.46b0
# opentelemetry-instrumentation-httpx>=0.46b0
